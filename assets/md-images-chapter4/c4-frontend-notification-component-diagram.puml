@startuml
title LowCortisol Platform - Notification Frontend Component Diagram

!define C4Puml https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master
!includeurl C4Puml/C4_Component.puml

LAYOUT_WITH_LEGEND()

Container(spa, "Single Page Application", "Vue / Vite", "Provides alert and notification management.")
Container(api, "Backend API", "ASP.NET Core Web API", "Provides alert, incident, notification channel, delivery and mitigation endpoints.")

Container_Boundary(notification, "Notification") {

  Component(presentation, "Presentation", "Vue Screens / Components", "Contains alert screen, active alert cards, incident panel, channel summary, mitigation modal and alert actions.")

  Component(application, "Application", "Pinia Store / Facade", "Coordinates alert loading, incident creation, incident assignment, mitigation requests, alert resolution and channel status.")

  Component(domain, "Domain", "Domain Model", "Contains Alert, Incident, IncidentAction, NotificationChannel and AlertDelivery frontend concepts.")

  Component(infrastructure, "Infrastructure", "API Service", "Consumes notification backend endpoints and adapts responses to frontend models.")
}

Rel(spa, presentation, "Uses notification screens", "Vue Router")

Rel(presentation, application, "Dispatches notification actions")
Rel(application, domain, "Uses notification models")
Rel(application, infrastructure, "Delegates API operations")

Rel(infrastructure, api, "Consumes notification endpoints", "HTTPS/JSON")

@enduml
